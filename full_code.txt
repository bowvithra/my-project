<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Business Recommender - Admin Dashboard</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            overflow-x: hidden;
        }

        .navbar {
            background: rgba(255, 255, 255, 0.95) !important;
            backdrop-filter: blur(20px);
            box-shadow: 0 8px 32px rgba(0,0,0,0.1);
            padding: 1rem 0;
            position: sticky;
            top: 0;
            z-index: 1000;
        }
        
        .navbar a {
            color: #333 !important;
            font-weight: 500;
            margin: 0 1.5rem;
            transition: all 0.3s ease;
            text-decoration: none;
        }
        
        .navbar a:hover {
            color: #667eea !important;
            transform: translateY(-2px);
        }
        

        
        .nav-links a:hover,
        .nav-links a.active {
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }

        /* Main Content */
        .main-content {
            margin-top: 100px;
            padding: 2rem;
            max-width: 1400px;
            margin-left: auto;
            margin-right: auto;
        }

        /* Dashboard Header */
        .dashboard-header {
            text-align: center;
            margin-bottom: 3rem;
            color: white;
        }

        .dashboard-header h1 {
            font-size: 2.8rem;
            margin-bottom: 0.5rem;
            background: linear-gradient(45deg, #fff, #f0f0f0);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .dashboard-header p {
            font-size: 1.2rem;
            opacity: 0.9;
            max-width: 700px;
            margin: 0 auto;
        }

        /* Stats Cards */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .stat-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            padding: 1.8rem;
            border-radius: 20px;
            text-align: center;
            transition: all 0.3s ease;
            border: 1px solid rgba(255,255,255,0.2);
            color: white;
            cursor: pointer;
        }

        .stat-card:hover {
            transform: translateY(-10px);
            background: rgba(255, 255, 255, 0.2);
            box-shadow: 0 20px 40px rgba(0,0,0,0.2);
        }

        .stat-icon {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            opacity: 0.9;
        }

        .stat-number {
            font-size: 2.2rem;
            font-weight: bold;
            margin-bottom: 0.3rem;
        }

        .stat-label {
            font-size: 1rem;
            opacity: 0.8;
        }

        /* Charts Section */
        .charts-section {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 2rem;
            margin-bottom: 2rem;
        }

        .chart-container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            padding: 2rem;
            border-radius: 20px;
            border: 1px solid rgba(255,255,255,0.2);
        }

        .chart-title {
            color: white;
            font-size: 1.3rem;
            margin-bottom: 1.5rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .chart-wrapper {
            position: relative;
            height: 300px;
        }

        /* Recent Activity */
        .recent-activity {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            padding: 2rem;
            border-radius: 20px;
            border: 1px solid rgba(255,255,255,0.2);
        }

        .activity-item {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1rem 0;
            border-bottom: 1px solid rgba(255,255,255,0.1);
            transition: all 0.3s ease;
        }

        .activity-item:hover {
            background: rgba(255,255,255,0.05);
            padding-left: 1rem;
        }

        .activity-item:last-child {
            border-bottom: none;
        }

        .activity-icon {
            width: 50px;
            height: 50px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            color: white;
            flex-shrink: 0;
        }

        .activity-content h4 {
            color: white;
            margin-bottom: 0.3rem;
            font-size: 1rem;
        }

        .activity-time {
            color: rgba(255,255,255,0.7);
            font-size: 0.9rem;
        }

        /* Admin Actions */
        .admin-actions {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 1.2rem;
        }

        .action-btn {
            padding: 1.2rem 1.5rem;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            border: 1px solid rgba(255,255,255,0.3);
            border-radius: 15px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.8rem;
            backdrop-filter: blur(10px);
            font-size: 0.95rem;
        }

        .action-btn:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
        }

        /* Responsive */
        @media (max-width: 1024px) {
            .charts-section {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 768px) {
            .nav-links {
                gap: 0.5rem;
            }
            .nav-links a {
                padding: 0.6rem 1rem;
                font-size: 0.9rem;
            }
            .main-content {
                padding: 1rem;
            }
            .dashboard-header h1 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <!-- NAVBAR -->
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand fw-bold fs-3 text-primary" href="home">
            
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="home"><i class="fas fa-home"></i> Home</a>
                <a class="nav-link active" href="recommendations"><i class="fas fa-robot"></i> AI Recommendations</a>
                <a class="nav-link" href="innovationhub"><i class="fas fa-lightbulb"></i> Innovation Hub</a>
                <a class="nav-link" href="admin"><i class="fas fa-cog"></i> Admin</a>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Dashboard Header -->
        <div class="dashboard-header">
            <h1><i class="fas fa-chart-line"></i> Admin Dashboard</h1>
            <p>Real-time analytics & management for your AI-Driven Location-Based Business Recommendation System</p>
        </div>

        <!-- Stats Cards -->
        <div class="stats-grid">
            <div class="stat-card" data-stat="12450">
                <div class="stat-icon"><i class="fas fa-users"></i></div>
                <div class="stat-number">12,450</div>
                <div class="stat-label">Active Users</div>
            </div>
            <div class="stat-card" data-stat="3247">
                <div class="stat-icon"><i class="fas fa-store"></i></div>
                <div class="stat-number">3,247</div>
                <div class="stat-label">Businesses</div>
            </div>
            <div class="stat-card" data-stat="89234">
                <div class="stat-icon"><i class="fas fa-thumbs-up"></i></div>
                <div class="stat-number">89K</div>
                <div class="stat-label">Recommendations</div>
            </div>
            <div class="stat-card" data-growth="24">
                <div class="stat-icon"><i class="fas fa-bolt"></i></div>
                <div class="stat-number">+24%</div>
                <div class="stat-label">Monthly Growth</div>
            </div>
            <div class="stat-card" data-stat="1247">
                <div class="stat-icon"><i class="fas fa-map-marker-alt"></i></div>
                <div class="stat-number">1,247</div>
                <div class="stat-label">Cities Covered</div>
            </div>
            <div class="stat-card" data-stat="98.7">
                <div class="stat-icon"><i class="fas fa-brain"></i></div>
                <div class="stat-number">98.7%</div>
                <div class="stat-label">AI Accuracy</div>
            </div>
        </div>

        <!-- Charts Section -->
        <div class="charts-section">
            <div class="chart-container">
                <div class="chart-title">
                    <i class="fas fa-chart-area"></i>
                    User Growth & Recommendations
                </div>
                <div class="chart-wrapper">
                    <canvas id="mainChart"></canvas>
                </div>
            </div>
            <div class="chart-container">
                <div class="chart-title">
                    <i class="fas fa-doughnut-chart"></i>
                    Business Categories
                </div>
                <div class="chart-wrapper">
                    <canvas id="doughnutChart"></canvas>
                </div>
            </div>
        </div>

        <div class="charts-section">
            <div class="recent-activity">
                <div class="chart-title">
                    <i class="fas fa-clock"></i>
                    Recent Activity
                </div>
                <div class="activity-item">
                    <div class="activity-icon" style="background: #667eea;">
                        <i class="fas fa-user-plus"></i>
                    </div>
                    <div class="activity-content">
                        <h4>New user registered in Mumbai</h4>
                        <div class="activity-time">Just now</div>
                    </div>
                </div>
                <div class="activity-item">
                    <div class="activity-icon" style="background: #f093fb;">
                        <i class="fas fa-thumbs-up"></i>
                    </div>
                    <div class="activity-content">
                        <h4>Restaurant approved</h4>
                        <div class="activity-time">2 min ago</div>
                    </div>
                </div>
                <div class="activity-item">
                    <div class="activity-icon" style="background: #4facfe;">
                        <i class="fas fa-map-marker-alt"></i>
                    </div>
                    <div class="activity-content">
                        <h4>New location: Bangalore added</h4>
                        <div class="activity-time">5 min ago</div>
                    </div>
                </div>
                <div class="activity-item">
                    <div class="activity-icon" style="background: #43e97b;">
                        <i class="fas fa-star"></i>
                    </div>
                    <div class="activity-content">
                        <h4>AI model accuracy improved to 98.7%</h4>
                        <div class="activity-time">12 min ago</div>
                    </div>
                </div>
            </div>
            <div class="chart-container">
                <div class="chart-title">
                    <i class="fas fa-tachometer-alt"></i>
                    System Performance
                </div>
                <div class="chart-wrapper">
                    <canvas id="barChart"></canvas>
                </div>
            </div>
        </div>

        <!-- Admin Actions -->
        <div class="admin-actions">
            <button class="action-btn">
                <i class="fas fa-users-cog"></i>
                Manage Users
            </button>
            <button class="action-btn">
                <i class="fas fa-building"></i>
                Businesses
            </button>
            <button class="action-btn">
                <i class="fas fa-robot"></i>
                AI Models
            </button>
            <button class="action-btn">
                <i class="fas fa-map"></i>
                Locations
            </button>
            <button class="action-btn">
                <i class="fas fa-chart-bar"></i>
                Analytics
            </button>
            <button class="action-btn">
                <i class="fas fa-cog"></i>
                Settings
            </button>
            <button class="action-btn">
                <i class="fas fa-download"></i>
                Export Data
            </button>
            <button class="action-btn">
                <i class="fas fa-broom"></i>
                Database Cleanup
            </button>
        </div>
    </div>

    <script>
        // Initialize Charts
        const ctx1 = document.getElementById('mainChart').getContext('2d');
        new Chart(ctx1, {
            type: 'line',
            data: {
                labels: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul'],
                datasets: [{
                    label: 'Active Users',
                    data: [12000, 19000, 15000, 22000, 28000, 32000, 12450],
                    borderColor: '#667eea',
                    backgroundColor: 'rgba(102, 126, 234, 0.1)',
                    tension: 0.4,
                    fill: true
                }, {
                    label: 'Recommendations',
                    data: [45000, 62000, 58000, 75000, 89000, 95000, 89234],
                    borderColor: '#f093fb',
                    backgroundColor: 'rgba(240, 147, 251, 0.1)',
                    tension: 0.4,
                    fill: true
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        labels: { 
                            color: 'white', 
                            font: { size: 14 },
                            padding: 20
                        }
                    }
                },
                scales: {
                    x: { 
                        ticks: { color: 'rgba(255,255,255,0.8)' }, 
                        grid: { color: 'rgba(255,255,255,0.1)' } 
                    },
                    y: { 
                        ticks: { color: 'rgba(255,255,255,0.8)' }, 
                        grid: { color: 'rgba(255,255,255,0.1)' } 
                    }
                },
                interaction: {
                    intersect: false,
                    mode: 'index'
                }
            }
        });

        // Doughnut Chart
        const ctx2 = document.getElementById('doughnutChart').getContext('2d');
        new Chart(ctx2, {
            type: 'doughnut',
            data: {
                labels: ['Restaurants', 'Retail Shops', 'Services', 'Entertainment'],
                datasets: [{
                    data: [45, 30, 15, 10],
                    backgroundColor: ['#667eea', '#764ba2', '#f093fb', '#4facfe'],
                    borderWidth: 0
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        position: 'bottom',
                        labels: { 
                            color: 'white', 
                            font: { size: 12 },
                            padding: 15
                        }
                    }
                },
                cutout: '60%'
            }
        });

        // Bar Chart
        const ctx3 = document.getElementById('barChart').getContext('2d');
                new Chart(ctx3, {
            type: 'bar',
            data: {
                labels: ['Response Time', 'AI Accuracy', 'User Sat.', 'Load Time'],
                datasets: [{
                    label: 'Performance %',
                    data: [95, 98.7, 94, 97],
                    backgroundColor: ['#667eea', '#764ba2', '#f093fb', '#4facfe'],
                    borderRadius: 8,
                    borderSkipped: false
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: { display: false }
                },
                scales: {
                    y: {
                        beginAtZero: true,
                        max: 100,
                        ticks: { 
                            color: 'rgba(255,255,255,0.8)',
                            callback: function(value) { return value + '%'; }
                        },
                        grid: { color: 'rgba(255,255,255,0.1)' }
                    },
                    x: { 
                        ticks: { color: 'rgba(255,255,255,0.8)' },
                        grid: { display: false }
                    }
                }
            }
        });

        // Interactive Features - Action Buttons
        document.querySelectorAll('.action-btn').forEach(btn => {
            btn.addEventListener('click', function() {
                const action = this.querySelector('i').classList[1];
                const actions = {
                    'fa-users-cog': '👥 User Management\n\n• View all users\n• Ban/Unban users\n• Analytics per user\n\nReady for backend!',
                    'fa-building': '🏢 Business Management\n\n• Approve listings\n• Verify locations\n• Category management\n\nIntegration ready!',
                    'fa-robot': '🤖 AI Models\n\n• Train new models\n• Update algorithms\n• Monitor performance\n• A/B Testing\n\nAI Dashboard!',
                    'fa-map': '🗺️ Location Management\n\n• Add new cities\n• Update coordinates\n• Geofencing\n• Heatmaps\n\nMap integration ready!',
                    'fa-chart-bar': '📊 Advanced Analytics\n\n• User behavior\n• Success rates\n• Location trends\n• Export reports\n\nPower BI ready!',
                    'fa-cog': '⚙️ System Settings\n\n• Configure AI\n• Manage categories\n• Set notifications\n• Backup settings\n\nAdmin config!',
                    'fa-download': '📥 Data Export\n\n• CSV/JSON export\n• Database backup\n• Reports generation\n• API endpoints\n\nExport ready!',
                    'fa-broom': '🧹 Database Cleanup\n\n• Remove duplicates\n• Archive old data\n• Optimize indexes\n• Run maintenance\n\nDB tools ready!'
                };
                
                alert(actions[action] || 'Feature clicked! 🚀');
            });
        });

        // Navbar Navigation
        document.querySelectorAll('.nav-links a').forEach(link => {
            link.addEventListener('click', function(e) {
                e.preventDefault();
                document.querySelector('.nav-links a.active').classList.remove('active');
                this.classList.add('active');
                
                // Simulate page transition
                document.body.style.opacity = '0.5';
                setTimeout(() => {
                    document.body.style.opacity = '1';
                }, 200);
            });
        });

        // Animate on Scroll
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        // Observe animated elements
        document.querySelectorAll('.stat-card, .chart-container, .recent-activity, .action-btn').forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(30px)';
            el.style.transition = 'all 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94)';
            observer.observe(el);
        });

        // Live Stats Counter Animation
        function animateStats() {
            document.querySelectorAll('.stat-card[data-stat]').forEach(card => {
                const target = parseFloat(card.dataset.stat);
                const current = parseFloat(card.querySelector('.stat-number').textContent.replace(/[^\d.]/g, ''));
                const increment = target > current ? 1 : -1;
                
                if (Math.abs(target - current) > 1) {
                    const newValue = (current + increment).toLocaleString();
                    card.querySelector('.stat-number').textContent = 
                        card.dataset.stat.includes('.') ? (current + increment).toFixed(1) + '%' : newValue;
                }
            });
        }

        // Update stats periodically (demo)
        let statInterval = setInterval(animateStats, 80);
        setTimeout(() => clearInterval(statInterval), 3000);

        // Real-time Activity Feed (Demo)
        const activities = [
            { icon: 'fa-user-plus', color: '#667eea', title: 'New user registered', city: 'Delhi', time: 'Just now' },
            { icon: 'fa-thumbs-up', color: '#f093fb', title: 'Business approved', type: 'Restaurant', time: '1 min ago' },
            { icon: 'fa-star', color: '#43e97b', title: 'AI model updated', metric: 'accuracy', time: '3 min ago' },
            { icon: 'fa-map-marker-alt', color: '#4facfe', title: 'New location added', city: 'Chennai', time: '5 min ago' },
            { icon: 'fa-chart-line', color: '#ff6b6b', title: 'Peak usage detected', metric: 'traffic', time: '10 min ago' },
            { icon: 'fa-bell', color: '#ffd93d', title: 'New notification rule', type: 'created', time: '15 min ago' }
        ];

        function addNewActivity() {
            const container = document.querySelector('.recent-activity');
            const firstActivity = container.querySelector('.activity-item:first-child');
            
            const activity = activities[Math.floor(Math.random() * activities.length)];
            const newActivity = document.createElement('div');
            newActivity.className = 'activity-item';
            newActivity.innerHTML = `
                <div class="activity-icon" style="background: ${activity.color};">
                    <i class="fas ${activity.icon}"></i>
                </div>
                <div class="activity-content">
                    <h4>${activity.title} ${activity.city || activity.type || activity.metric ? `(${activity.city || activity.type || activity.metric})` : ''}</h4>
                    <div class="activity-time">${activity.time}</div>
                </div>
            `;
            
            container.insertBefore(newActivity, firstActivity);
            
            // Keep only 4 activities
            if (container.querySelectorAll('.activity-item').length > 4) {
                container.removeChild(container.querySelector('.activity-item:last-child'));
            }
        }

        // Add activity every 8-12 seconds
        setInterval(addNewActivity, Math.random() * 4000 + 8000);

        // Stats card click handlers
        document.querySelectorAll('.stat-card').forEach(card => {
            card.addEventListener('click', function() {
                const label = this.querySelector('.stat-label').textContent;
                alert(`📈 ${label} Details\n\nClick to view detailed analytics for ${label.toLowerCase()}!\n\nBackend integration point.`);
            });
        });

        // Keyboard Shortcuts
        document.addEventListener('keydown', (e) => {
            if (e.ctrlKey && e.key === 'r') {
                e.preventDefault();
                location.reload();
            } else if (e.key === 'Escape') {
                // Close any modals (future feature)
                console.log('Escape pressed - Close modal');
            }
        });

        // Performance monitoring (demo)
        function updatePerformance() {
            const now = new Date();
            console.log(`📊 Dashboard Performance: ${now.toLocaleTimeString()}`);
        }
        setInterval(updatePerformance, 30000);

        console.log('🚀 AI Business Recommender - Admin Dashboard v2.0');
        console.log('✅ All features loaded successfully!');
        console.log('💡 Features: Interactive Charts | Live Stats | Real-time Activity | Responsive Design');
        console.log('🔗 Ready for production deployment!');
        console.log('⌨️ Shortcuts: Ctrl+R (Refresh)');
    </script>
</body>
</html>

---



<!DOCTYPE html>
<html>
<head>
    <title>AI Business Recommendation System</title>
    <link rel="stylesheet" href="css/style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>

<body>

<!-- NAVIGATION BAR -->
<div class="navbar">
    <a href="home" class="active">🏠 Home</a>
    <a href="recommendations">🤖 AI Recommendations</a>
    <a href="Innovationhub">💡 Innovation Hub</a>
    <a href="admin">⚙️ Admin</a>
</div>

<!-- HERO SECTION -->
<div class="hero">
    <h1>AI Business Finder</h1>
    <p>Discover the best businesses near you using AI</p>

    <div class="search-box">
        <input id="location" placeholder="Enter your location">
        <button onclick="goSearch()">Search</button>
    </div>
</div>

<!-- FEATURES -->
<section class="container">
<h2 class="feature-title">
<i class="fas fa-rocket"></i>Why Choose Our AI Platform?
</h2>


<div class="features-column">

<div class="card">
<i class="fas fa-brain"></i>
<h4>AI Matching</h4>
<p>AI analyzes your preferences to give the best business suggestions.</p>
</div>

<div class="card">
<i class="fas fa-map-marked-alt"></i>
<h4>Real Time Location</h4>
<p>Find businesses near your exact location.</p>
</div>

<div class="card">
<i class="fas fa-shield-alt"></i>
<h4>Trusted & Secure</h4>
<p>Your data is protected while using our platform.</p>
</div>

</div>
</section>

<!-- BUSINESS SECTION -->
<section class="container">
<h2 class="business-heading">
<i class="fas fa-star"></i>Popular Business Categories in Chennai
</h2>

<div id="businessGrid"></div>

</section>

<!-- FOOTER -->
<footer>
<p>© 2024 AI Business Recommendation System</p>
</footer>

<script>

function goSearch(){
let location=document.getElementById("location").value.trim();
if(location===""){
alert("Please enter location");
return;
}
localStorage.setItem("searchLocation",location);
window.location="recommendations";
}

const businesses=[
"Bakery","Medical Shop","Super Market","GroceryStore","Laundry Service",
"Clinic","Gym","Beauty Salon","Pet Care Center","StationeryStore",
"Clothing Store","Cafe","Restaurant","Electronics","Jewelry Shop",
"Shoe Store","Gift Shop","Mobile Shop","Printing Shop","Photo Studio",
"Co-working Space","Cloud Kitchen","Digital Marketing Agency",
"Electric vehicle Charging station","3D Printing Shop",
"Smart Vending machine hub","Home Automations","Drone Delivery",
"Water refill station","space tourism booking agency",
"Subscription toy library for kids","Tire Shop","Paint Store","Tiles & Flooring Shop",
"Underground urban farming","Dairy Products Shop","Tatto Studio",
"Fix-it cafe","refill station store","Car Wash Center",
"Clothing repair & customization","Book Store","Yoga Studio","sports Equipment Shop"

];

const grid=document.getElementById("businessGrid");

businesses.forEach((name)=>{

grid.innerHTML+=`
<div class="business-card" onclick="selectBusiness('${name}')">

<img src="/static/images/${name.replace(/\s+/g,'')}.jpg" 
onerror="this.src='/static/images/default.jpg'">

<div class="card-body">
<h6 class="business-title">${name}</h6>
</div>

</div>
`;

});

function selectBusiness(business){
document.getElementById("location").value=business;
goSearch();
}

</script>

</body>
</html>

---

Innovationhub

<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AI Business Recommender - Innovation Hub</title>

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">

<style>

:root{
--primary-gradient:linear-gradient(135deg,#667eea 0%,#764ba2 100%);
--success-gradient:linear-gradient(45deg,#28a745,#20c997);
--ai-gradient:linear-gradient(45deg,#ff6b6b,#ffa500);
}

*{font-family:'Poppins',sans-serif;}

body{
background:linear-gradient(135deg,#f5f7fa 0%,#c3cfe2 100%);
min-height:100vh;
}

.navbar-custom{
background:var(--primary-gradient);
box-shadow:0 10px 30px rgba(0,0,0,0.2);
padding:1rem 0;
}

.navbar-custom .nav-link{
color:white!important;
margin:0 1rem;
}

.ai-hero{
background:var(--primary-gradient);
color:white;
padding:4rem 0;
border-radius:0 0 50px 50px;
margin-bottom:3rem;
}

.location-finder{
background:white;
border-radius:20px;
box-shadow:0 20px 40px rgba(0,0,0,0.1);
padding:2.5rem;
margin-bottom:3rem;
}

.business-card{
border:none;
border-radius:20px;
overflow:hidden;
transition:.4s;
box-shadow:0 10px 30px rgba(0,0,0,0.1);
position:relative;
}

.business-card:hover{
transform:translateY(-10px);
box-shadow:0 20px 40px rgba(0,0,0,0.2);
}

.business-card img{
height:400px;
object-fit:cover;
}

.ai-badge{
position:absolute;
top:15px;
right:15px;
background:var(--ai-gradient);
color:white;
padding:.5rem 1rem;
border-radius:20px;
font-size:.8rem;
}

.investment-badge{
background:var(--success-gradient);
color:white;
padding:.4rem 1rem;
border-radius:20px;
font-size:.9rem;
}

.success-rate{
background:#e8f5e9;
border:2px solid #a5d6a7;
color:#2e7d32;
padding:.3rem .8rem;
border-radius:15px;
font-size:.85rem;
}

</style>
</head>

<body>

<nav class="navbar navbar-expand-lg navbar-custom fixed-top">
<div class="container">

<a class="navbar-brand text-white fw-bold" href="home">
<i class="fas fa-robot"></i>  ChennaiAI
</a>

<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
<span class="navbar-toggler-icon"></span>
</button>

<div class="collapse navbar-collapse" id="navbarNav">

<ul class="navbar-nav ms-auto">

<li class="nav-item">
<a class="nav-link" href="home">
<i class="fas fa-home"></i> Home
</a>
</li>

<li class="nav-item">
<a class="nav-link" href="recommendations">
<i class="fas fa-robot"></i> AI Recommendations
</a>
</li>

<li class="nav-item">
<a class="nav-link active" href="innovationhub">
<i class="fas fa-lightbulb"></i> Innovation Hub
</a>
</li>

<li class="nav-item">
<a class="nav-link" href="admin">
<i class="fas fa-cog"></i> Admin
</a>
</li>

</ul>

</div>

</div>
</nav>

<section class="ai-hero">
<div class="container position-relative">
<div class="row align-items-center">

<div class="col-lg-8">

<h1 class="display-4 fw-bold mb-4">
<i class="fas fa-bolt me-3"></i>AI-Powered Innovation Hub
</h1>

<p class="lead mb-4 opacity-90">
Location-based business recommendations with real-time market analysis, competitor mapping, and profitability predictions
</p>

<div class="d-flex align-items-center gap-3">

<span class="badge bg-light text-dark fs-6 px-3 py-2">
<i class="fas fa-brain me-1"></i>Powered by AI
</span>

<span class="badge bg-warning text-dark fs-6 px-3 py-2">
<i class="fas fa-map-marker-alt me-1"></i>10K+ Locations Analyzed
</span>

</div>

</div>

</div>
</div>
</section>

<div class="container">

<div class="row g-4 mb-5" id="businessGrid">
</div>

</div>



<div class="modal fade" id="businessModal">

<div class="modal-dialog modal-lg">

<div class="modal-content">

<div class="modal-header bg-primary text-white">

<h5 class="modal-title">
AI Business Analysis
</h5>

<button class="btn-close btn-close-white"
data-bs-dismiss="modal"></button>

</div>

<div class="modal-body"
id="modalContent">

</div>

</div>
</div>
</div>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>


<script>

const businessData=[

{name:"Cloud Kitchen",investment:"₹5L-₹10L",successRate:92,icon:"fa-utensils",img:"/static/images/cloudkitchen.jpg",profitability:"High",locationFit:95},

{name:"EV Charging Station",investment:"₹10L-₹20L",successRate:88,icon:"fa-charging-station",img:"/static/images/electricvehiclechargingstation.jpg",profitability:"Very High",locationFit:92},

{name:"3D Printing Shop",investment:"₹6L-₹12L",successRate:85,icon:"fa-cube",img:"/static/images/3dprintingshop.jpg",profitability:"Medium-High",locationFit:89},

{name:"Drone Delivery",investment:"₹8L-₹15L",successRate:78,icon:"fa-helicopter",img:"/static/images/dronedelivery.jpg",profitability:"High",locationFit:87},

{name:"Smart Vending hub",investment:"₹4L-₹8L",successRate:94,icon:"fa-box",img:"/static/images/smartvendingmachinehub.jpg",profitability:"Very High",locationFit:96},

{name:"E-Bike Rental",investment:"₹5L-₹9L",successRate:90,icon:"fa-bicycle",img:"/static/images/electricbikerentalshop.jpg",profitability:"High",locationFit:93},

{name:"Home Automation",investment:"₹6L-₹12L",successRate:87,icon:"fa-home",img:"/static/images/homeautomations.jpg",profitability:"Medium-High",locationFit:91},

{name:"Internet Cafe",investment:"₹3L-₹6L",successRate:96,icon:"fa-wifi",img:"/static/images/Internetcafe.jpg",profitability:"Very High",locationFit:98},

{name:"Software Training",investment:"₹7L-₹15L",successRate:89,icon:"fa-laptop-code",img:"/static/images/softwaretraningcentre.jpg",profitability:"High",locationFit:94},

{name:"Online Grocery",investment:"₹5L-₹10L",successRate:91,icon:"fa-shopping-basket",img:"/static/images/onlinegrocerystore.jpg",profitability:"High",locationFit:90},

{name:"Space Tourism Booking Agency",investment:"₹8L-₹20L",successRate:82,icon:"fa-rocket",img:"/static/images/spacetourismbookingagency.jpg",profitability:"High",locationFit:85},

{name:"Subscription Toy Library for Kids",investment:"₹3L-₹7L",successRate:91,icon:"fa-puzzle-piece",img:"/static/images/subscriptiontoylibraryforkids.jpg",profitability:"High",locationFit:92},

{name:"Fix-it cafe",investment:"₹2L-₹5L",successRate:88,icon:"fa-tools",img:"/static/images/fix-itcafe.jpg",profitability:"Medium-High",locationFit:90},

{name:"Underground Urban Farming",investment:"₹6L-₹12L",successRate:86,icon:"fa-seedling",img:"/static/images/undergroundurbanfarming.jpg",profitability:"High",locationFit:87},

{name:"Clothing Repair & Customization",investment:"₹2L-₹6L",successRate:93,icon:"fa-tshirt",img:"/static/images/clothingrepair&customization.jpg",profitability:" very High",locationFit:95},

{name:"Refill Station Store",investment:"₹4L-₹9L",successRate:90,icon:"fa-recycle",img:"/static/images/refillstationstore.jpg",profitability:"High",locationFit:91}

];


function generateRecommendations(city){

const grid=document.getElementById("businessGrid");

grid.innerHTML="";

businessData.forEach(business=>{

const card=createBusinessCard(business,city);

grid.appendChild(card);

});

}


function createBusinessCard(business,city){

const card=document.createElement("div");

card.className="col-lg-4 col-md-6";

card.innerHTML=`

<div class="card business-card">

<div class="ai-badge">
AI Score ${business.locationFit}%
</div>

<img src="${business.img.split('/').map(part => encodeURIComponent(part)).join('/')}" class="card-img-top">

<div class="card-body d-flex flex-column p-4">

<h5 class="fw-bold">${business.name}</h5>

<p class="card-text text-muted">
${business.name} is a profitable opportunity in ${city}.
</p>

<div class="d-flex justify-content-between mb-3">

<span class="investment-badge">
${business.investment}
</span>

<span class="success-rate">
${business.successRate}%
</span>

</div>

<button class="btn btn-outline-primary mt-auto"
onclick="showBusinessDetails('${business.name}','${business.investment}','${business.successRate}','${business.profitability}','${city}')">

View AI Analysis

</button>

</div>
</div>
`;

return card;

}


function showBusinessDetails(name,investment,successRate,profitability,city){

const modalContent=document.getElementById("modalContent");

modalContent.innerHTML=`

<h4>${name}</h4>

<p>City : ${city}</p>

<p>Investment : ${investment}</p>

<p>Success Rate : ${successRate}%</p>

<p>Profitability : ${profitability}</p>

`;

const modal=new bootstrap.Modal(document.getElementById("businessModal"));

modal.show();

}

document.addEventListener("DOMContentLoaded", function() {
generateRecommendations("India");
});

</script>

</body>
</html>
---

login.html

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AuthApp - Login & Register</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/@emailjs/browser@4/dist/email.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/imask@7.6.1/dist/imask.min.js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        .glass-effect {
            backdrop-filter: blur(20px);
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        .form-input {
            transition: all 0.3s ease;
        }
        .form-input:focus {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
        }
        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            transition: all 0.3s ease;
        }
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 15px 35px rgba(102, 126, 234, 0.4);
        }
        .phone-input {
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 24 24' stroke='currentColor'%3e%3cpath stroke-linecap='round' stroke-linejoin='round' stroke-width='2' d='M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z'/%3e%3c/svg%3e");
            background-repeat: no-repeat;
            background-position: 16px center;
            background-size: 20px;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-indigo-500 via-purple-500 to-pink-500 min-h-screen flex items-center justify-center p-4">
    <div class="max-w-md w-full space-y-8">
        <!-- Logo/Header -->
        <div class="text-center">
            <div class="w-20 h-20 bg-white/20 rounded-2xl flex items-center justify-center mx-auto mb-6 backdrop-blur-sm">
                <i class="fas fa-shield-alt text-3xl text-white"></i>
            </div>
            <h2 class="text-4xl font-bold text-white mb-2">AI Bussiness Finder</h2>
            <p class="text-white/80 text-lg">Welcome back! Please sign in to your account</p>
        </div>

        <!-- Tabs -->
        <div class="glass-effect rounded-2xl p-1">
            <div class="flex bg-white/10 rounded-xl">
                <button id="loginTab" class="flex-1 py-3 px-6 text-center font-semibold text-white rounded-xl bg-white/20 transition-all duration-300">Login</button>
                <button id="registerTab" class="flex-1 py-3 px-6 text-center font-semibold text-white/70 rounded-xl transition-all duration-300">Register</button>
            </div>
        </div>

        <!-- Login Form -->
        <div id="loginForm" class="glass-effect rounded-3xl p-8 space-y-6">
            <form id="loginFormSubmit">
                <div>
                    <label class="block text-white font-semibold mb-3">Email Address</label>
                    <input type="email" id="loginEmail" required 
                           class="form-input w-full px-5 py-4 bg-white/20 border border-white/30 rounded-2xl text-white placeholder-white/70 focus:outline-none focus:ring-4 focus:ring-white/30" 
                           placeholder="Enter your email">
                </div>
                <div>
                    <label class="block text-white font-semibold mb-3">Password</label>
                    <input type="password" id="loginPassword" required 
                           class="form-input w-full px-5 py-4 bg-white/20 border border-white/30 rounded-2xl text-white placeholder-white/70 focus:outline-none focus:ring-4 focus:ring-white/30" 
                           placeholder="Enter your password">
                </div>
                <br><br>
                <button type="submit" class="btn-primary w-full py-4 px-8 rounded-2xl text-xl font-bold text-white shadow-2xl">
                    <span id="loginBtnText">Sign In</span>
                    <i id="loginSpinner" class="fas fa-spinner fa-spin hidden ml-2"></i>
                </button>
            </form>
            <div class="text-center">
                <a href="#" class="text-white/70 hover:text-white transition-colors">Forgot Password?</a>
            </div>
        </div>

        <!-- Register Form (NOW WITH PHONE NUMBER!) -->
        <div id="registerForm" class="glass-effect rounded-3xl p-8 space-y-6 hidden">
            <form id="registerFormSubmit">
                <div>
                    <label class="block text-white font-semibold mb-3">
                        <i class="fas fa-user mr-2"></i>Full Name
                    </label>
                    <input type="text" id="regName" required 
                           class="form-input w-full pl-12 px-5 py-4 bg-white/20 border border-white/30 rounded-2xl text-white placeholder-white/70 focus:outline-none focus:ring-4 focus:ring-white/30" 
                           placeholder="Enter your full name">
                </div>
                
                <div>
                    <label class="block text-white font-semibold mb-3">
                        <i class="fas fa-envelope mr-2"></i>Email Address
                    </label>
                    <input type="email" id="regEmail" required 
                           class="form-input w-full pl-12 px-5 py-4 bg-white/20 border border-white/30 rounded-2xl text-white placeholder-white/70 focus:outline-none focus:ring-4 focus:ring-white/30" 
                           placeholder="Enter your email">
                </div>
                
                <div>
                    <label class="block text-white font-semibold mb-3">
                        <i class="fas fa-phone mr-2"></i>Phone Number
                    </label>
                    <input type="tel" id="regPhone" required 
                           class="phone-input form-input w-full pl-14 px-5 py-4 bg-white/20 border border-white/30 rounded-2xl text-white placeholder-white/70 focus:outline-none focus:ring-4 focus:ring-white/30" 
                           placeholder="+1 (555) 123-4567">
                </div>
                
                <div>
                    <label class="block text-white font-semibold mb-3">
                        <i class="fas fa-lock mr-2"></i>Password
                    </label>
                    <input type="password" id="regPassword" required 
                           class="form-input w-full pl-12 px-5 py-4 bg-white/20 border border-white/30 rounded-2xl text-white placeholder-white/70 focus:outline-none focus:ring-4 focus:ring-white/30" 
                           placeholder="Create a password (8+ characters)">
                </div>
                <br><br>
                
                <button type="submit" class="btn-primary w-full py-4 px-8 rounded-2xl text-xl font-bold text-white shadow-2xl">
            
                    <span id="regBtnText">Create Account</span>
                    <i id="regSpinner" class="fas fa-spinner fa-spin hidden ml-2"></i>
                </button>
            </form>
            <div class="text-xs text-white/60 text-center pt-4 border-t border-white/20">
                By signing up, you agree to our Terms of Service and Privacy Policy
            </div>
        </div>

        <!--button-->

        <button onclick="sendWelcomeEmail('Test','youremail@gmail.com')">
    
</button>

        <!-- Success/Error Messages -->
        <div id="messageContainer" class="glass-effect rounded-2xl p-6 hidden">
            <div id="messageContent"></div>
        </div>
    </div>

    <script>
        // Replace with your EmailJS credentials
        const EMAILJS_SERVICE_ID = 'service_ty5nmpc';
        const EMAILJS_TEMPLATE_ID_LOGIN = 'YOUR_LOGIN_TEMPLATE_ID';
        const EMAILJS_TEMPLATE_ID_REGISTER = 'YOUR_REGISTER_TEMPLATE_ID';
        const EMAILJS_PUBLIC_KEY = 'zWqZVWYGhQZu6F9Bc';

        emailjs.init(EMAILJS_PUBLIC_KEY);

        // Phone number mask
        const phoneInput = document.getElementById('regPhone');
        const phoneMask = IMask(phoneInput, {
            mask: '+{91} {000} {000} {0000}',
            lazy: false
        });

        // Tab switching
        const loginTab = document.getElementById('loginTab');
        const registerTab = document.getElementById('registerTab');
        const loginForm = document.getElementById('loginForm');
        const registerForm = document.getElementById('registerForm');

        loginTab.addEventListener('click', () => {
            loginTab.classList.add('bg-white/20');
            loginTab.classList.remove('text-white/70');
            registerTab.classList.remove('bg-white/20');
            registerTab.classList.add('text-white/70');
            loginForm.classList.remove('hidden');
            registerForm.classList.add('hidden');
        });

        registerTab.addEventListener('click', () => {
            registerTab.classList.add('bg-white/20');
            registerTab.classList.remove('text-white/70');
            loginTab.classList.remove('bg-white/20');
            loginTab.classList.add('text-white/70');
            registerForm.classList.remove('hidden');
            loginForm.classList.add('hidden');
        });

        // Show message function
        function showMessage(message, type = 'success') {
            const container = document.getElementById('messageContainer');
            const content = document.getElementById('messageContent');
            
            container.className = `glass-effect rounded-2xl p-6 ${type === 'success' ? 'border-l-4 border-green-400' : 'border-l-4 border-red-400'}`;
            content.innerHTML = `
                <div class="flex items-center">
                    <i class="fas ${type === 'success' ? 'fa-check-circle text-green-400 text-2xl mr-3' : 'fa-exclamation-circle text-red-400 text-2xl mr-3'}"></i>
                    <div>
                        <p class="text-white font-semibold">${type === 'success' ? 'Success!' : 'Error!'}</p>
                        <p class="text-white/80">${message}</p>
                    </div>
                </div>
            `;
            container.classList.remove('hidden');
            
            setTimeout(() => {
                container.classList.add('hidden');
            }, 5000);
        }

        // Password strength indicator
        const passwordInput = document.getElementById('regPassword');
        passwordInput.addEventListener('input', function() {
            const strength = this.value.length >= 8 ? 'strong' : 'weak';
            this.style.boxShadow = strength === 'strong' 
                ? '0 0 0 3px rgba(34, 197, 94, 0.5)' 
                : '0 0 0 3px rgba(239, 68, 68, 0.5)';
        });

        // Login form submission
        document.getElementById('loginFormSubmit').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const email = document.getElementById('loginEmail').value;
            const password = document.getElementById('loginPassword').value;
            const btnText = document.getElementById('loginBtnText');
            const spinner = document.getElementById('loginSpinner');

            btnText.classList.add('hidden');
            spinner.classList.remove('hidden');

            try {
                await emailjs.send("service_ty5nwpc", "template_1uxpy13", {
                    to_email: email,
                    message: `Login attempt detected for ${email}. IP: ${getClientIP()}`,
                    timestamp: new Date().toLocaleString()
                });

                showMessage('Login successful! Email notification sent.', 'success');
                setTimeout(() => {
                    alert('Redirecting to dashboard...');
                }, 1000);

            } catch (error) {
                console.error('EmailJS error:', error);
                showMessage('Failed to send login notification. Please try again.', 'error');
            } finally {
                btnText.classList.remove('hidden');
                spinner.classList.add('hidden');
            }
        });

        // Register form submission (NOW INCLUDES PHONE!)
        document.getElementById('registerFormSubmit').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const name = document.getElementById('regName').value;
            const email = document.getElementById('regEmail').value;
            const phone = phoneMask.unmaskedValue || document.getElementById('regPhone').value;
            const password = document.getElementById('regPassword').value;
            const btnText = document.getElementById('regBtnText');
            const spinner = document.getElementById('regSpinner');

            // Basic phone validation
            const phoneRegex = /^\+?[\d\s\-\$\$]{10,}$/;
            if (!phoneRegex.test(phone)) {
                showMessage('Please enter a valid phone number.', 'error');
                return;
            }

            btnText.classList.add('hidden');
            spinner.classList.remove('hidden');

            try {
                await emailjs.send("service_ty5nwpc", "template_1uxpy13", {
                    to_name: name,
                    to_email: email,
                    to_phone: phone,
                    timestamp: new Date().toLocaleString()
                });

                showMessage(`Welcome ${name}! Account created successfully. Check your email for confirmation. 📧`, 'success');
                document.getElementById('registerFormSubmit').reset();
                phoneMask.updateValue('');

            } catch (error) {
                console.error('EmailJS error:', error);
                showMessage('Registration failed. Please try again.', 'error');
            } finally {
                btnText.classList.remove('hidden');
                spinner.classList.add('hidden');
            }
        });

        function getClientIP() {
            return 'Demo IP: 192.168.1.1';
        }
        </script>

        <script src="https://cdn.jsdelivr.net/npm/emailjs-com@3/dist/email.min.js"></script>
    <script>
(function(){
    emailjs.init("zWqZVWYGhQZu6F9Bc"); // 🔥 replace
})();

function sendWelcomeEmail(name, email) {
    emailjs.send("service_ty5nwpc", "template_1uxpy13", {
        to_name: name,
        to_email: email
    })
    .then(function(response) {
        alert("✅ Email sent successfully!");
    }, function(error) {
        alert("❌ Failed to send email");
    });
}
    </script>
</body>
</html>

---
recommendations

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ChennaiAI - Smart Business Recommendations</title>
    
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        * {
            font-family: 'Poppins', sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            position: relative;
        }
        
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="rgba(255,255,255,0.1)"/><circle cx="75" cy="75" r="1" fill="rgba(255,255,255,0.05)"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
            pointer-events: none;
            z-index: 1;
        }
        
        .navbar {
            background: rgba(255, 255, 255, 0.95) !important;
            backdrop-filter: blur(20px);
            box-shadow: 0 8px 32px rgba(0,0,0,0.1);
            padding: 1rem 0;
            position: sticky;
            top: 0;
            z-index: 1000;
        }
        
        .navbar a {
            color: #333 !important;
            font-weight: 500;
            margin: 0 1.5rem;
            transition: all 0.3s ease;
            text-decoration: none;
        }
        
        .navbar a:hover {
            color: #667eea !important;
            transform: translateY(-2px);
        }
        
        .hero-section {
            background: rgba(255,255,255,0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            border: 1px solid rgba(255,255,255,0.2);
            padding: 3rem;
            margin: 2rem 0;
        }
        
        .ai-badge {
            background: linear-gradient(45deg, #ff6b6b, #feca57);
            color: white;
            padding: 0.5rem 1.5rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.9rem;
            box-shadow: 0 4px 15px rgba(255,107,107,0.4);
        }
        
        .form-control, .form-select {
            border: 2px solid rgba(255,255,255,0.3);
            border-radius: 15px;
            padding: 0.75rem 1.25rem;
            background: rgba(255,255,255,0.9);
            transition: all 0.3s ease;
        }
        
        .form-control:focus, .form-select:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102,126,234,0.25);
            background: white;
            transform: translateY(-2px);
        }
        
        .btn-primary {
            background: linear-gradient(45deg, #667eea, #764ba2);
            border: none;
            border-radius: 15px;
            padding: 0.75rem 2rem;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            box-shadow: 0 8px 25px rgba(102,126,234,0.4);
        }
        
        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(102,126,234,0.6);
        }
        
        .business-card {
            background: rgba(255,255,255,0.95);
            backdrop-filter: blur(20px);
            border: none;
            border-radius: 20px;
            overflow: hidden;
            transition: all 0.4s ease;
            height: 100%;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
        }
        
        .business-card:hover {
            transform: translateY(-15px);
            box-shadow: 0 30px 60px rgba(0,0,0,0.2);
        }
        
        .business-card img {
            height: 300px;
            object-fit: cover;
            transition: transform 0.4s ease;
        }
        
        .business-card:hover img {
            transform: scale(1.05);
        }
        
        .stats-badge {
            background: linear-gradient(45deg, #4ecdc4, #44a08d);
            color: white;
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
        }
        
        .loading-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(102,126,234,0.9);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            backdrop-filter: blur(10px);
        }
        
        .results-header {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            padding: 2rem;
            border-radius: 20px;
            margin-bottom: 2rem;
            text-align: center;
        }
        
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }
        
        .pulse {
            animation: pulse 2s infinite;
        }
    </style>
</head>

<body>
    <!-- Loading Overlay -->
    <div id="loadingOverlay" class="loading-overlay">
        <div class="text-center text-white">
            <i class="fas fa-brain fa-4x mb-3 pulse"></i>
            <h3>AI Analyzing Your Location...</h3>
            <div class="spinner-border text-white mt-3" role="status"></div>
        </div>
    </div>

    <!-- NAVBAR -->
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand fw-bold fs-3 text-primary" href="home">
                <i class="fas fa-robot"></i> ChennaiAI
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="home"><i class="fas fa-home"></i> Home</a>
                <a class="nav-link active" href="recommendations"><i class="fas fa-robot"></i> AI Recommendations</a>
                <a class="nav-link" href="innovationhub"><i class="fas fa-lightbulb"></i> Innovation Hub</a>
                <a class="nav-link" href="admin"><i class="fas fa-cog"></i> Admin</a>
            </div>
        </div>
    </nav>

    <!-- HERO SECTION -->
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-lg-8 text-center">
                <div class="ai-badge mb-4">
                    <i class="fas fa-brain"></i> Powered by Advanced Machine Learning
                </div>
                <h1 class="display-4 fw-bold text-white mb-4">
                    Smart Business Recommendations
                    <span class="text-warning">for Chennai</span>
                </h1>
                <p class="lead text-white-50 mb-5">
                    Enter your location details and our AI will recommend the <strong>best businesses</strong> 
                    based on area type, population, and footfall analysis.
                </p>
            </div>
        </div>

        <!-- INPUT FORM -->
        <div class="row justify-content-center mt-5">
            <div class="col-lg-6">
                <div class="hero-section p-5">
                    <h3 class="text-center mb-4 text-primary">
                        <i class="fas fa-map-marker-alt"></i> Location Analysis
                    </h3>
                    
                    <div class="mb-4">
                        <label class="form-label fw-bold text-white">📍 Location</label>
                        <input id="location" class="form-control form-control-lg" 
                               placeholder="Eg: T. Nagar, Anna Nagar, Velachery..." required>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-4">
                            <label class="form-label fw-bold text-white">🏠 Area Type</label>
                            <select id="areaType" class="form-select form-select-lg">
                                <option>Residential</option>
                                <option>Commercial</option>
                                <option>Mixed</option>
                                <option>Industrial</option>
                            </select>
                        </div>
                        <div class="col-md-6 mb-4">
                            <label class="form-label fw-bold text-white">👥 Population</label>
                            <select id="population" class="form-select form-select-lg">
                                <option>Low Population</option>
                                <option>Medium Population</option>
                                <option>High Population</option>
                            </select>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-4">
                            <label class="form-label fw-bold text-white">🚶 Footfall</label>
                            <select id="footfall" class="form-select form-select-lg">
                                <option>Low Footfall</option>
                                <option>Medium Footfall</option>
                                <option>High Footfall</option>
                            </select>
                        </div>
                        <div class="col-md-6 mb-4">
                            <label class="form-label fw-bold text-white">💰 Budget</label>
                            <select id="budget" class="form-select form-select-lg">
                                <option>Low Investment</option>
                                <option>Medium Investment</option>
                                <option>High Investment</option>
                            </select>
                        </div>
                    </div>

                    <button id="recommendBtn" onclick="getRecommendations()" 
                            class="btn btn-primary btn-lg w-100 py-3">
                        <i class="fas fa-rocket me-2"></i>
                        <span id="btnText">Generate AI Recommendations</span>
                        <span id="loadingSpinner" class="spinner-border spinner-border-sm ms-2" 
                              style="display:none;"></span>
                    </button>

                    <div class="mt-3 text-center">
                        <small class="text-white-50">
                            <i class="fas fa-shield-alt me-1"></i>
                            100% AI Powered • Data Privacy Guaranteed
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- RESULTS SECTION -->
    <div id="resultsSection" class="container mt-5" style="display:none;">
        <div class="results-header">
            <h2 class="mb-3">
                <i class="fas fa-trophy"></i> Top AI Recommendations
            </h2>
            <p class="mb-0 opacity-75">Success Rate: <span class="fw-bold">94.7%</span> | 
                Analyzed <span id="analysisCount">0</span> factors</p>
        </div>
        
        <div id="recommendationCards" class="row g-4"></div>
        
        <div class="text-center mt-5">
            <button class="btn btn-outline-light btn-lg px-5" onclick="getRecommendations()">
                <i class="fas fa-redo me-2"></i>Analyze Again
            </button>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Enhanced Recommendation Function
        async function getRecommendations() {
            // Input validation
            const location = document.getElementById("location").value.trim();
            if (!location) {
                showNotification("Please enter a location!", "warning");
                return;
            }

            // Show loading
            showLoading(true);

            try {
                const response = await fetch("http://127.0.0.1:5000/recommend", {
                    method: "POST",
                    headers: { "Content-Type": "application/json" },
                    body: JSON.stringify({
                        location: location,
                        area_type: document.getElementById("areaType").value,
                        population_density: document.getElementById("population").value,
                        footfall: document.getElementById("footfall").value,
                        budget: document.getElementById("budget").value
                    })
                });

                if (!response.ok) throw new Error(`HTTP ${response.status}`);

                const data = await response.json();
                
                // Display results
                displayResults(data);
                showNotification(`${data.recommendations.length} business ideas generated!`, "success");

            } catch (error) {
                console.error('Error:', error);
                showNotification("AI service temporarily unavailable. Please try again.", "error");
            } finally {
                showLoading(false);
            }
        }

        function displayResults(data) {
            document.getElementById("resultsSection").style.display = "block";
            document.getElementById("analysisCount").textContent = data.analysis_factors || 12;
            
            const container = document.getElementById("recommendationCards");
            container.innerHTML = "";

            data.recommendations.forEach((business, index) => {
                const imageName = business.toLowerCase().replace(/\s+/g,'');
                const card = createBusinessCard(business, imageName, index);
                container.innerHTML += card;
            });

            // Smooth scroll to results
            document.getElementById("recommendationCards").scrollIntoView({ 
                behavior: "smooth" 
            });
        }

        function createBusinessCard(business, imageName, index) {
            const successRates = ['92%', '87%', '95%', '89%', '91%'];
            return `
                <div class="col-lg-4 col-md-6">
                    <div class="business-card h-100">
                        <img src="./static/${imageName.replace(/\s+/g,'').toLowerCase()}.jpg" 
                             class="card-img-top" 
                             alt="${business}"
                             onerror="this.src='https://via.placeholder.com/400x220/667eea/ffffff?text=${encodeURIComponent(business)}'">
                        <div class="card-body p-4">
                            <div class="d-flex justify-content-between align-items-center mb-3">
                                <span class="stats-badge">
                                    <i class="fas fa-chart-line me-1"></i>
                                    ${successRates[index % successRates.length]}
                                </span>
                                <span class="badge bg-success">
                                    #${index + 1}
                                </span>
                            </div>
                            <h5 class="card-title fw-bold text-primary">${business}</h5>
                            <p class="card-text text-muted">
                                Perfect match for your location parameters. 
                                High ROI potential based on AI analysis.
                            </p>
                            <div class="d-flex gap-2 mt-3">
                                <button class="btn btn-outline-primary btn-sm flex-fill">
                                    <i class="fas fa-eye"></i> Details
                                </button>
                                <button class="btn btn-primary btn-sm">
                                    <i class="fas fa-save"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            `;
        }

        function showLoading(show) {
            const overlay = document.getElementById("loadingOverlay");
            const btn = document.getElementById("recommendBtn");
            const btnText = document.getElementById("btnText");
            const spinner = document.getElementById("loadingSpinner");
            
            if (show) {
                overlay.style.display = "flex";
                btn.disabled = true;
                btnText.style.display = "none";
                spinner.style.display = "inline-block";
            } else {
                overlay.style.display = "none";
                btn.disabled = false;
                btnText.style.display = "inline";
                spinner.style.display = "none";
            }
        }

        function showNotification(message, type = 'info') {
            // Simple toast notification
            const toast = document.createElement('div');
            toast.className = `alert alert-${type === 'error' ? 'danger' : type} alert-dismissible fade show position-fixed`;
            toast.style.cssText = 'top: 20px; right: 20px; z-index: 9998; min-width: 300px;';
            toast.innerHTML = `
                ${message}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            `;
            document.body.appendChild(toast);
            
            setTimeout(() => {
                toast.remove();
            }, 5000);
        }

        // Enter key support
        document.getElementById("location").addEventListener("keypress", function(event) {
            if (event.key === "Enter") {
                getRecommendations();
            }
        });

        // Preload default image
        const link = document.createElement('link');
        link.rel = 'preload';
        link.href = 'https://via.placeholder.com/400x220/667eea/ffffff';
        link.as = 'image';
        document.head.appendChild(link);
    </script>
</body>
</html>

---
welcome.html

<!DOCTYPE html>
<html>
<head>

<title>Welcome</title>

<style>

body{
margin:0;
height:100vh;
display:flex;
justify-content:center;
align-items:center;
font-family:Arial;
background: linear-gradient(135deg,#0f2027,#203a43,#2c5364);
color:white;
overflow:hidden;
}

/* container */

.box{
text-align:center;
animation: fadeIn 3s ease-in-out;
}

/* welcome text */

h1{
font-size:42px;
margin-bottom:10px;
animation: slideDown 3s ease;
}

p{
font-size:18px;
opacity:0.8;
}

/* loading spinner */

.loader{
margin:30px auto;
border:6px solid rgba(255,255,255,0.2);
border-top:6px solid white;
border-radius:50%;
width:60px;
height:60px;
animation: spin 3s linear infinite;
}

/* animations */

@keyframes spin{
0%{transform:rotate(0deg);}
100%{transform:rotate(360deg);}
}

@keyframes fadeIn{
from{opacity:0;}
to{opacity:1;}
}

@keyframes slideDown{
from{
transform:translateY(-40px);
opacity:0;
}
to{
transform:translateY(5);
opacity:1;
}
}

</style>

</head>

<body>

<div class="box">

<h1 id="welcomeText"></h1>

<p>AI Driven Location Based Business Recommendation System</p>

<div class="loader"></div>

<p>Loading your experience...</p>

</div>

<script>

let user = localStorage.getItem("loggedUser")

document.getElementById("welcomeText").innerHTML = "Welcome, " + user

setTimeout(function(){

window.location="index.html"

},5000)

</script>

</body>
</html>
---

app.py

from flask import Flask, request, jsonify
from flask_cors import CORS
import pandas as pd

app = Flask(__name__)
CORS(app)

# Load location coordinates
locations_df = pd.read_csv("locations.csv")

# Business list
BUSINESSES = [
"Bakery","Medical Shop","Super Market","Grocery Store","Laundry Service",
"Clinic","Gym","Beauty Salon","Pet Care Center","Stationery Store",
"Clothing Store","Cafe","Restaurant","Electronics","Jewelry Shop",
"Shoe Store","Gift Shop","Mobile Shop","Printing Shop","Photo Studio",
"Co-working Space","Cloud Kitchen","Digital Marketing Agency",
"Electric Vehicle Charging Station","3D Printing Shop",
"Smart Vending Machine Hub","Online Grocery Store",
"Home Automation Store","Drone Delivery","Electric Bike Rental shop",
"Internet Cafe","Software Training Center","Fast Food shop",
"Ice Cream Shop","Sweet Shop","Tea Shop","Juice Shop","Meatshop",
"Water refill station","space tourism booking agency",
"Subscription toy library for kids","Tire Shop","Paint Store","Tiles & Flooring Shop",
"Underground urban farming","Dairy Products Shop","Tatto Studio",
"Fix-it cafe","refill station store","Car Wash Center",
"Clothing repair & customization","Book Store","Yoga Studio","sports Equipment Shop"
]


@app.route("/")
def home():
    return "AI Business Recommendation API Running"


@app.route("/recommend", methods=["POST"])
def recommend():

    data = request.get_json()

    # Inputs from frontend
    location = data.get("location","").lower()
    area_type = data.get("area_type","").lower()
    population = data.get("population_density","").lower()
    footfall = data.get("footfall","").lower()

    # Get coordinates
    row = locations_df[locations_df["location"].str.lower() == location]

    if not row.empty:
        lat = float(row.iloc[0]["lat"])
        lon = float(row.iloc[0]["lon"])
    else:
        lat = 13.0827
        lon = 80.2707

    results = []

    for business in BUSINESSES:

        score = 0

        # Residential logic
        if area_type == "residential":
            if business in [
                "Bakery","Medical Shop","Super Market","Meatshop","Shoe Store","Clinic"
                "Grocery Store","Clinic","Gym","Laundry Service","Online Grocery Dark Store",
                "Beauty Salon","Pet care centre","Fast food shop","printing shop","Restaurant"

            ]:
                score += 4

        # Commercial logic
        if area_type == "commercial":
            if business in [
                "Clothing Store","Jewelry Shop","Cloud Kitchen",
                "Restaurant","Electronics Shop","Gift Shop","Mobile Shop",
                "Photo studio","Ice cream shop","Electric Vehicle Charging Station",
                "Smart Vending Machine Hub","Photo Studio","3D Printing Shop"

            ]:
                score += 4

        # Mixed / IT area logic
        if area_type == "mixed":
            if business in [
                "Cafe","Co-working Space","Software Training Center"
                "Cloud Kitchen","Internet Cafe",
                "Co-working Space","Digital Marketing Agency","Home Automation Store"
                "Drone Delivery Service","Electric Bike Rental shop"
            ]:
                score += 4

        # Population influence
        if population == "high population":
            if business in [
                "Super Market","Restaurant","Clothing Store","Cafe","Clinic",
                "Drone Delivery Service","Digital Marketing Agency","Smart Vending Machine Hub"
        
            ]:
                score += 2

        # Footfall influence
        if footfall == "high footfall":
            if business in [
                "Cafe","Restaurant","Juice Shop",
                "Fast Food Center","Ice Cream Shop","Clinic",
                "Electric Bike Rental shop","Smart Vending Machine Hub"
            ]:
                score += 2

        results.append({
            "business": business,
            "score": score
        })

    # Sort by score
    results = sorted(results, key=lambda x: x["score"], reverse=True)

    # Top 10 businesses
    recommendations = [r["business"] for r in results[:10]]

    return jsonify({
        "location": location,
        "latitude": lat,
        "longitude": lon,
        "recommendations": recommendations
    })


if __name__ == "__main__":
    app.run(debug=True)
	
---

generate_data.py

import pandas as pd

# Load locations
locations = pd.read_csv("locations.csv")

businesses = [
    "Bakery","Super Market","Cafe","Clothing Store","Book Store",
    "Restaurant","Medical Shop","Pet Care Center","Cloud Kitchen",
    "Co-working Space"
]

rows = []

for _, row in locations.iterrows():
    for business in businesses:
        rows.append({
            "location": row["location"],
            "area_type": row["area_type"],
            "population_density": row["population_density"],
            "footfall": row["footfall"],
            "rent_level": row["rent_level"],
            "competition": row["competition"],
            "business": business,
            "success": 1 if business in ["Bakery","Cafe","Super Market"] else 0
        })

df = pd.DataFrame(rows)

df.to_csv("data.csv", index=False)

print("data.csv generated successfully")

---

locations.csv

location,area_type,population_density,footfall,rent_level,competition,lat,lon
Tambaram,residential,7000,5500,low,medium,12.9249,80.1000
Velachery,residential,7600,6200,medium,medium,12.9756,80.2206
T Nagar,commercial,9000,8500,high,high,13.0418,80.2341
Anna Nagar,commercial,8200,7000,high,high,13.0878,80.2102
Adyar,residential,7500,6400,medium,medium,13.0067,80.2575
Porur,mixed,6500,5200,medium,low,13.0382,80.1565
Guindy,commercial,8300,7800,high,high,13.0100,80.2120
OMR,commercial,8700,8000,high,medium,12.9120,80.2295
Perungudi,commercial,8000,7500,high,medium,12.9600,80.2450
Sholinganallur,mixed,7200,6800,medium,medium,12.9010,80.2279
Medavakkam,residential,6900,5300,low,medium,12.9229,80.1817
Chromepet,residential,7100,6000,medium,medium,12.9516,80.1462
Pallavaram,mixed,6800,5900,medium,medium,12.9675,80.1491
Kodambakkam,commercial,8200,7200,high,high,13.0526,80.2253
Nungambakkam,commercial,8500,7600,high,high,13.0604,80.2425
Egmore,commercial,8400,7400,high,high,13.0732,80.2609
Mylapore,commercial,8300,7300,high,medium,13.0336,80.2697
Besant Nagar,residential,7200,6500,medium,medium,13.0003,80.2668
Tambaram East,residential,6900,5400,low,medium,12.9279,80.1150
Tambaram West,residential,6800,5300,low,medium,12.9220,80.0950
Ambattur,industrial,7500,6200,medium,medium,13.1143,80.1548
Avadi,mixed,7000,5600,low,medium,13.1147,80.1018
Poonamallee,mixed,6600,5000,low,low,13.0489,80.1090
Koyambedu,commercial,8800,8200,high,high,13.0736,80.1943
Vadapalani,commercial,8300,7100,high,high,13.0500,80.2120

---

package.json

{
  "name": "proapp-auth",
  "version": "1.0.0",
  "description": "Professional Authentication System with Email Notifications",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": ["auth", "email", "professional", "react-like"],
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "bcryptjs": "^2.4.3",
    "nodemailer": "^6.9.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}

---

server.js

const express = require('express');
const mongoose = require('mongoose');
const cors = require('cors');
const bcrypt = require('bcryptjs');
const nodemailer = require('nodemailer');
const path = require('path');
require('dotenv').config();

const app = express();
app.use(express.json({ limit: '10mb' }));
app.use(cors());
app.use(express.static('public'));

// MongoDB
mongoose.connect(process.env.MONGODB_URI || 'mongodb://localhost:27017/real-auth-app', {
  useNewUrlParser: true,
  useUnifiedTopology: true,
}).then(() => console.log('✅ MongoDB Connected'))
  .catch(err => console.error('❌ MongoDB Error:', err));

// Professional Email Template
const transporter = nodemailer.createTransporter({
  service: 'gmail',
  auth: {
    user: process.env.EMAIL_USER,
    pass: process.env.EMAIL_PASS
  }
});

// User Schema
const userSchema = new mongoose.Schema({
  name: { type: String, required: true },
  email: { type: String, required: true, unique: true },
  password: { type: String, required: true },
  phone: { type: String, required: true },
  dob: { type: Date, required: true },
  avatar: { type: String, default: '' },
  isActive: { type: Boolean, default: true },
  createdAt: { type: Date, default: Date.now }
});

const User = mongoose.model('User', userSchema);

// Email Templates
const emailTemplates = {
  register: (user) => `
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body style="margin: 0; padding: 0; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: #f6f9fc;">
    <table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0" style="background: #f6f9fc;">
        <tr>
            <td style="padding: 40px 20px;">
                <table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0" style="max-width: 600px; margin: 0 auto; background: white; border-radius: 16px; box-shadow: 0 20px 40px rgba(0,0,0,0.08); overflow: hidden;">
                    <!-- Header -->
                    <tr>
                        <td style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); padding: 40px 30px; text-align: center;">
                            <h1 style="margin: 0; color: white; font-size: 28px; font-weight: 700;">Welcome to ProApp</h1>
                            <p style="margin: 8px 0 0 0; color: rgba(255,255,255,0.9); font-size: 16px;">Your account has been created successfully!</p>
                        </td>
                    </tr>
                    <!-- Content -->
                    <tr>
                        <td style="padding: 40px 30px;">
                            <h2 style="color: #1a1a1a; font-size: 24px; margin-bottom: 20px;">Hello ${user.name}! 🎉</h2>
                            <div style="background: #f8f9fa; padding: 24px; border-radius: 12px; border-left: 4px solid #667eea; margin-bottom: 24px;">
                                <h3 style="color: #333; margin: 0 0 12px 0; font-size: 18px;">📋 Account Details</h3>
                                <table style="width: 100%; font-size: 15px;">
                                    <tr><td style="color: #666; padding-right: 20px;">Name:</td><td><strong>${user.name}</strong></td></tr>
                                    <tr><td style="color: #666; padding-right: 20px;">Email:</td><td><strong>${user.email}</strong></td></tr>
                                    <tr><td style="color: #666; padding-right: 20px;">Phone:</td><td><strong>${user.phone}</strong></td></tr>
                                    <tr><td style="color: #666; padding-right: 20px;">Joined:</td><td><strong>${new Date().toLocaleDateString()}</strong></td></tr>
                                </table>
                            </div>
                            <p style="color: #666; line-height: 1.6; font-size: 16px;">
                                Thank you for joining ProApp. We're excited to have you on board!
                            </p>
                        </td>
                    </tr>
                    <!-- Footer -->
                    <tr>
                        <td style="background: #f8f9fa; padding: 30px; text-align: center; border-top: 1px solid #e9ecef;">
                            <p style="color: #6c757d; font-size: 14px; margin: 0;">
                                © 2024 ProApp. All rights reserved.<br>
                                This is an automated message. Please do not reply.
                            </p>
                        </td>
                    </tr>
                </table>
            </td>
        </tr>
    </table>
</body>
</html>
  `,
  
  login: (user) => `
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body style="margin: 0; padding: 0; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: #f6f9fc;">
    <table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0" style="background: #f6f9fc;">
        <tr>
            <td style="padding: 40px 20px;">
                <table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0" style="max-width: 600px; margin: 0 auto; background: white; border-radius: 16px; box-shadow: 0 20px 40px rgba(0,0,0,0.08); overflow: hidden;">
                    <!-- Header -->
                    <tr>
                        <td style="background: linear-gradient(135deg, #28a745 0%, #20c997 100%); padding: 40px 30px; text-align: center;">
                            <h1 style="margin: 0; color: white; font-size: 28px; font-weight: 700;">Login Successful</h1>
                            <p style="margin: 8px 0 0 0; color: rgba(255,255,255,0.9); font-size: 16px;">Welcome back ${user.name}!</p>
                        </td>
                    </tr>
                    <!-- Content -->
                    <tr>
                        <td style="padding: 40px 30px;">
                            <div style="background: #d4edda; padding: 24px; border-radius: 12px; border-left: 4px solid #28a745; margin-bottom: 24px;">
                                <h3 style="color: #155724; margin: 0 0 12px 0; font-size: 18px;">✅ Security Notification</h3>
                                <p style="color: #155724; margin: 0; font-size: 15px;">
                                    You logged in at <strong>${new Date().toLocaleString()}</strong>
                                </p>
                            </div>
                            <p style="color: #666; line-height: 1.6; font-size: 16px;">
                                If you didn't login, please secure your account immediately.
                            </p>
                        </td>
                    </tr>
                    <!-- Footer -->
                    <tr>
                        <td style="background: #f8f9fa; padding: 30px; text-align: center; border-top: 1px solid #e9ecef;">
                            <p style="color: #6c757d; font-size: 14px; margin: 0;">
                                © 2024 ProApp. All rights reserved.
                            </p>
                        </td>
                    </tr>
                </table>
            </td>
        </tr>
    </table>
</body>
</html>
  `
};

// REGISTER
app.post('/api/register', async (req, res) => {
  try {
    const { name, email, password, phone, dob } = req.body;

    const existingUser = await User.findOne({ email });
    if (existingUser) {
      return res.status(409).json({ 
        success: false, 
        message: 'User already exists with this email!' 
      });
    }

    const hashedPassword = await bcrypt.hash(password, 12);
    const user = new User({ name, email, password: hashedPassword, phone, dob: new Date(dob) });
    await user.save();

    // Send Professional Email
    transporter.sendMail({
      from: `"ProApp" <${process.env.EMAIL_USER}>`,
      to: email,
      subject: '🎉 Welcome to ProApp - Account Created Successfully!',
      html: emailTemplates.register(user)
    });

    res.json({ 
      success: true, 
      message: 'Account created successfully! 🎉 Check your email for confirmation.',
      user: { name: user.name, email: user.email }
    });

  } catch (error) {
    console.error('Register Error:', error);
    res.status(500).json({ success: false, message: 'Registration failed. Please try again.' });
  }
});

// LOGIN
app.post('/api/login', async (req, res) => {
  try {
    const { email, password } = req.body;

    const user = await User.findOne({ email });
    if (!user || !user.isActive) {
      return res.status(401).json({ success: false, message: 'Invalid credentials!' });
    }

    const isMatch = await bcrypt.compare(password, user.password);
    if (!isMatch) {
      return res.status(401).json({ success: false, message: 'Invalid credentials!' });
    }

    // Send Login Notification
    transporter.sendMail({
      from: `"ProApp Security" <${process.env.EMAIL_USER}>`,
      to: email,
      subject: '🔐 Login Successful - ProApp',
      html: emailTemplates.login(user)
    });

    res.json({ 
      success: true, 
      message: 'Login successful! 🔐 Check your email notification.',
      user: { 
        id: user._id,
        name: user.name, 
        email: user.email,
        phone: user.phone 
      },
      token: 'login-success-' + Date.now() // Real JWT in production
    });

  } catch (error) {
    console.error('Login Error:', error);
    res.status(500).json({ success: false, message: 'Login failed. Please try again.' });
  }
});

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
  console.log(`🚀 ProApp running on http://localhost:${PORT}`);
  console.log(`📧 Email notifications: ${process.env.EMAIL_USER}`);
});

----

train_model.py

import pandas as pd
import numpy as np
import joblib

from sklearn.ensemble import RandomForestClassifier
from sklearn.preprocessing import LabelEncoder

# Load dataset
df = pd.read_csv("locations.csv")

# Possible businesses
businesses = [
    "Cafe","Restaurant","Bakery","Super Market","Medical Shop",
    "Electronics Shop","Clothing Store","Gym","Pet Shop","Laundry",
    "Juice Shop","Book Store"
]

rows = []

# Create training rows
for _, row in df.iterrows():

    for business in businesses:

        success = 0

        if row["footfall"] > 7000 and business in ["Restaurant","Clothing Store","Electronics Shop"]:
            success = 1

        elif row["population_density"] > 7000 and business in ["Cafe","Bakery","Super Market"]:
            success = 1

        elif row["rent_level"] == "low" and business in ["Laundry","Pet Shop","Gym"]:
            success = 1

        rows.append({
            "area_type": row["area_type"],
            "population_density": row["population_density"],
            "footfall": row["footfall"],
            "rent_level": row["rent_level"],
            "competition": row["competition"],
            "business": business,
            "success": success
        })

train_df = pd.DataFrame(rows)

# Encode categorical values
encoders = {}

for col in ["area_type","rent_level","competition","business"]:
    le = LabelEncoder()
    train_df[col] = le.fit_transform(train_df[col])
    encoders[col] = le

X = train_df.drop("success",axis=1)
y = train_df["success"]

# Train model
model = RandomForestClassifier(n_estimators=200)
model.fit(X,y)

# Save model
joblib.dump(model,"business_success_model.pkl")

# Save encoders
joblib.dump(encoders,"encoders.pkl")

print("Model trained successfully")

---

